var a = 5;
var b = 6;
function cong(s1, s2) //Khai báo hàm cong, s1, s2 là 2 tham số đầu vào
{
    return s1 + s2; // trả về tổng s1 và s2
}
console.log(cong(a, b)); // gọi hàm cong và in ra console

// kết quả: 11

